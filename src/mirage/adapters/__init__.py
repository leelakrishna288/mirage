"""Pipeline adapters. Each one is an alternative RagPipeline implementation.

The point of the RagPipeline protocol is that mirage measures a pipeline without
caring how it was built. These adapters exist to prove that, not to decorate it.
"""
